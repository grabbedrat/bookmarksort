export function callLLMAPI(){
    // Call the LLM API to categorize the bookmark
    // This is a placeholder for the actual API call
    return new Promise(resolve => {
        setTimeout(() => {
        resolve('API call result');
        }, 1000);
    });
}